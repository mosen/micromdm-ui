'use strict';

import * as devices from '../actions/api/devices';

const initialState = {

};

export default function errors (state = initialState, action) {
  switch (action.type) {

    default:
      return state;
  }
}
